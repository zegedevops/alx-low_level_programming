#include "main.h"

/**
 * print_numbers - checks digits (0 through 9).
 *
 * Return: Always 0.
 */

void print_numbers(void)
{
	int c;

	for (c = 48; c <= 57; c++)
	{
		_putchar(c);
	}
		_putchar('\n');
}
