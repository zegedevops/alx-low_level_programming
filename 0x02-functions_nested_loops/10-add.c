#include "main.h"

/**
 * add - adds two integers and returns the result
 * @y: int to be added to z
 * @z: int to be added to y
 * Return: sum of y and z
 */
int add(int y, int z)
{
	int a;

	a = y + z;

	return (a);
}
